name: TXW817 diagnostic build and package

on:
  workflow_dispatch:

concurrency:
  group: txw817-diagnostic-package
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow temporarily held while the deterministic TXW817 build is updated."
